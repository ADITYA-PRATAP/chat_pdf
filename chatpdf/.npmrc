; Quiet transitive npm deprecation / funding / audit noise during install.
; These warnings come from dependencies-of-dependencies (aws-sdk, drizzle-kit,
; @langchain/community) and are informational only — they never affect the build.
loglevel=error
fund=false
audit=false
